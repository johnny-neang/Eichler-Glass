{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "drizzle-kit push --force && vite build",
  "outputDirectory": "dist/public",
  "framework": null,
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api" },
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
